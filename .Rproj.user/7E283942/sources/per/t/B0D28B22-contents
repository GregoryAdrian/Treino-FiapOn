plot(mtcars$mpg)

# Conjunto de dados
mtcars

# Mostra colunas disponiveis
mtcars$wt

# Calcula a media
mean(mtcars$wt)

# Calcula o desvio Padrão
sd(mtcars$wt)

(3 + 4) / 2
c(3, 4)

# Atribuindo valores
lista <- c(3, 4)

# Media
mean(lista)

install.packages("plyr")
library(plyr)

# Calcula a Frenquencia 
count(mtcars$wt)

plot (mtcars$mpg, mtcars$wt)
